export default defineNuxtRouteMiddleware((to, from) => {
    // const token = useCookie<string>('token')
    // if(!token.value && to.path !=='/' && to.path !=='/sign-up'){
    //     return navigateTo('/')
    // }
    // if(!token.value && to.path !== '/') {
    //     return navigateTo('/')
    // }
    // else if(token.value && to.path === '/') {
    //     return navigateTo('/home')
    // }
})